import { Button } from '@mui/material';

const StartPage = () => {
    return (
        <div>
            <Button href="/login">Log in</Button>
            <Button href="/signup">Sign up</Button>
        </div >
    );
};

export { StartPage };